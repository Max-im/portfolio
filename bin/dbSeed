#!/bin/bash

export PGPASSWORD='max'

user='max'
db="portfolio"

echo "start initing DB $db"

dropdb -U $user $db
createdb -U $user $db

psql -U $user $db < './src/server/db/dump.sql'

echo "db $db configured"
